/**
 * parser.js — Validates and sanitizes AI-returned JSON
 *
 * Re-exports from the shared SPP inverse engine library.
 */

export { parseAIResponse } from '../../../spp-lib/spp-inverse-engine.js';
